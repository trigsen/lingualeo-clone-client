{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "config/*"
    ]
  },
  "scripts": {
    "refresh": "lerna clean && rm -rf node_modules && yarn install && lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^6.3.0"
  }
}
